<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yunchuan.medical.mapper.MedicalRecordMapper">

    <resultMap id="BaseResultMap" type="com.yunchuan.medical.entity.MedicalRecord">
        <id column="id" property="id"/>
        <result column="patient_id" property="patientId"/>
        <result column="doctor_id" property="doctorId"/>
        <result column="visit_time" property="visitTime"/>
        <result column="diagnosis" property="diagnosis"/>
        <result column="treatment" property="treatment"/>
        <result column="chief_complaint" property="chiefComplaint"/>
        <result column="present_illness" property="presentIllness"/>
        <result column="past_history" property="pastHistory"/>
        <result column="physical_exam" property="physicalExam"/>
        <result column="remark" property="remark"/>
        <result column="status" property="status"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <sql id="Base_Column_List">
        mr.id, mr.patient_id, mr.doctor_id, mr.visit_time, mr.diagnosis, mr.treatment,
        mr.chief_complaint, mr.present_illness, mr.past_history, mr.physical_exam, mr.remark,
        mr.status, mr.create_time, mr.update_time
    </sql>

    <select id="selectById" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM medical_record mr
        WHERE mr.id = #{id}
    </select>

    <select id="selectByPatientId" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM medical_record mr
        WHERE mr.patient_id = #{patientId}
        ORDER BY mr.create_time DESC
    </select>

    <select id="selectAll" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM medical_record mr
        ORDER BY mr.create_time DESC
    </select>

    <insert id="insert" parameterType="com.yunchuan.medical.entity.MedicalRecord">
        INSERT INTO medical_record (
            id, patient_id, doctor_id, visit_time, diagnosis, treatment,
            chief_complaint, present_illness, past_history, physical_exam, remark,
            status, create_time, update_time
        )
        VALUES (
            #{id}, #{patientId}, #{doctorId}, #{visitTime}, #{diagnosis}, #{treatment},
            #{chiefComplaint}, #{presentIllness}, #{pastHistory}, #{physicalExam}, #{remark},
            #{status}, #{createTime}, #{updateTime}
        )
    </insert>

    <update id="update" parameterType="com.yunchuan.medical.entity.MedicalRecord">
        UPDATE medical_record
        <set>
            <if test="patientId != null">patient_id = #{patientId},</if>
            <if test="doctorId != null">doctor_id = #{doctorId},</if>
            <if test="visitTime != null">visit_time = #{visitTime},</if>
            <if test="diagnosis != null">diagnosis = #{diagnosis},</if>
            <if test="treatment != null">treatment = #{treatment},</if>
            <if test="status != null">status = #{status},</if>
            <if test="updateTime != null">update_time = #{updateTime}</if>
        </set>
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM medical_record WHERE id = #{id}
    </delete>

</mapper> 