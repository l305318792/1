<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yunchuan.medical.mapper.ScheduleMapper">
    
    <resultMap id="BaseResultMap" type="com.yunchuan.medical.entity.Schedule">
        <id column="id" property="id"/>
        <result column="doctor_id" property="doctorId"/>
        <result column="department_id" property="departmentId"/>
        <result column="schedule_date" property="scheduleDate"/>
        <result column="period" property="period"/>
        <result column="max_appointments" property="maxAppointments"/>
        <result column="appointed_count" property="appointedCount"/>
        <result column="status" property="status"/>
        <result column="remark" property="remark"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <select id="selectByQuery" resultMap="BaseResultMap">
        SELECT * FROM schedule
        <where>
            <if test="doctorId != null and doctorId != ''">
                AND doctor_id = #{doctorId}
            </if>
            <if test="startDate != null">
                AND schedule_date >= #{startDate}
            </if>
            <if test="endDate != null">
                AND schedule_date &lt;= #{endDate}
            </if>
            <if test="period != null and period != ''">
                AND period = #{period}
            </if>
            <if test="status != null and status != ''">
                AND status = #{status}
            </if>
        </where>
        ORDER BY schedule_date ASC, period ASC
    </select>

    <select id="selectById" resultMap="BaseResultMap">
        SELECT * FROM schedule WHERE id = #{id}
    </select>
    
    <select id="selectByDoctorAndDate" resultMap="BaseResultMap">
        SELECT * FROM schedule 
        WHERE doctor_id = #{doctorId} 
        AND schedule_date = #{date}
    </select>
    
    <select id="selectByDoctor" resultMap="BaseResultMap">
        SELECT * FROM schedule 
        WHERE doctor_id = #{doctorId}
        AND schedule_date BETWEEN #{startDate} AND #{endDate}
    </select>
    
    <select id="selectByDoctorAndDateAndPeriod" resultMap="BaseResultMap">
        SELECT * FROM schedule 
        WHERE doctor_id = #{doctorId} 
        AND schedule_date = #{date} 
        AND period = #{period}
    </select>
    
    <select id="countByDoctorAndDate" resultType="int">
        SELECT COUNT(*) FROM schedule 
        WHERE doctor_id = #{doctorId} 
        AND schedule_date = #{date} 
        AND period = #{period}
    </select>
    
    <select id="selectAll" resultMap="BaseResultMap">
        SELECT * FROM schedule 
    </select>
    
    <insert id="insert" parameterType="com.yunchuan.medical.entity.Schedule">
        INSERT INTO schedule (
            id, doctor_id, department_id, schedule_date, period,
            max_appointments, appointed_count, status, remark,
            create_time, update_time
        ) VALUES (
            #{id}, #{doctorId}, #{departmentId}, #{scheduleDate}, #{period},
            #{maxAppointments}, #{appointedCount}, #{status}, #{remark},
            #{createTime}, #{updateTime}
        )
    </insert>
    
    <update id="update">
        UPDATE schedule
        SET status = #{status},
            update_time = #{updateTime}
        WHERE id = #{id}
    </update>
    
    <update id="incrementAppointedCount">
        UPDATE schedule 
        SET appointed_count = appointed_count + 1,
            update_time = NOW()
        WHERE id = #{id} 
        AND appointed_count &lt; max_appointments
    </update>
    
    <update id="decrementAppointedCount">
        UPDATE schedule 
        SET appointed_count = appointed_count - 1,
            update_time = NOW()
        WHERE id = #{id} 
        AND appointed_count &gt; 0
    </update>
    
    <delete id="deleteById">
        DELETE FROM schedule WHERE id = #{id}
    </delete>

    <insert id="batchInsert">
        INSERT INTO schedule (
            id, doctor_id, department_id, schedule_date, period, 
            max_appointments, appointed_count, status, remark, 
            create_time, update_time
        ) VALUES 
        <foreach collection="list" item="item" separator=",">
            (
                #{item.id}, #{item.doctorId}, #{item.departmentId}, 
                #{item.scheduleDate}, #{item.period}, 
                #{item.maxAppointments}, #{item.appointedCount}, 
                #{item.status}, #{item.remark}, 
                #{item.createTime}, #{item.updateTime}
            )
        </foreach>
    </insert>

    <update id="batchUpdateStatus">
        UPDATE schedule 
        SET status = #{status},
            update_time = NOW()
        WHERE id IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </update>

    <delete id="batchDelete">
        DELETE FROM schedule 
        WHERE id IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>
</mapper> 