# 运城市移动医疗咨询平台部署说明

## 1. 环境要求
- JDK 21
- MySQL 9.2
- Maven 3.9+

## 2. 数据库准备
1. 创建数据库：
```sql
CREATE DATABASE medical DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
```

2. 执行数据库脚本：
   - 先执行 `schema.sql`（创建表结构）
   - 再执行 `medical_backup.sql`（导入数据）

## 3. 配置修改
修改 `application.yml` 中的数据库配置：
```yaml
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/medical
    username: root
    password: 123456
```

## 4. 启动应用
```bash
# 方式一：直接运行JAR
java -jar medical-1.0.0.jar

# 方式二：指定配置文件运行
java -jar medical-1.0.0.jar --spring.config.location=application.yml
```

## 5. 验证部署
1. 访问 Swagger 文档：http://localhost:8080/swagger-ui.html
2. 使用 Postman 测试接口

## 6. 常见问题
1. 端口占用：修改 `application.yml` 中的 `server.port`
2. 数据库连接：检查数据库配置和防火墙设置
3. 内存不足：添加 JVM 参数 `-Xmx2g`

## 7. 联系方式
如有问题请联系技术支持。 